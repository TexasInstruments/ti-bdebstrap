---
mmdebstrap:
  architectures:
    - arm64
  mode: auto
  keyrings:
    - /usr/share/keyrings/ti-debpkgs.gpg
    - /usr/share/keyrings/debian-archive-keyring.gpg
  suite: bookworm
  variant: standard
  components:
    - main
    - contrib
    - non-free-firmware
  packages:
    - build-essential
    - gpg
    - curl
    - firmware-ti-connectivity
    - init
    - iproute2
    - less
    - libdrm-dev
    - libpam-systemd
    - locales
    - neofetch
    - network-manager
    - net-tools
    - openssh-server
    - sudo
    - vim
    - k3conf
    - linux-headers-6.1.46-k3
    - linux-image-6.1.46-k3
    - linux-libc-dev
    - firmware-ti-ipc-am62
    - firmware-cnm-wave
    - firmware-ti-prueth-am64
    - firmware-ti-pruhsr-am64
    - firmware-ti-prusw-am64
  mirrors:
    - http://deb.debian.org/debian
  setup-hooks:
      # Setup TI Debian Package Repository
    - 'mkdir -p $1/etc/apt/sources.list.d/'
    - 'wget https://raw.githubusercontent.com/sskartheekadivi/ti-debian-ppa/main/ti-debian-ppa.sources -P $1/etc/apt/sources.list.d/'
    - 'echo -e "Package: *\nPin: origin sskartheekadivi.github.io/ti-debian-ppa\nPin-Priority: 1001\n" > $1/etc/apt/preferences.d/ti-debian-ppa'
      # Install kernel post install script
    - 'mkdir -p $1/etc/kernel/postinst.d/'
    - 'echo "PWD = $PWD"'
    - 'upload target/kernel/postinst.d/cp-kernel-and-overlays /etc/kernel/postinst.d/cp-kernel-and-overlays'
    - 'chmod a+x $1/etc/kernel/postinst.d/cp-kernel-and-overlays'
  essential-hooks:
    # FIXME: Find a better workaround instead of sleep
    - 'sleep 10' # workaround for /proc resource busy unable to umount issue
  customize-hooks:
      # Remove passwd for root user
    - 'chroot "$1" passwd --delete root'
      # Fix apt install mandb permission issue
    - 'chroot "$1" chown -R man: /var/cache/man/'
    - 'chroot "$1" chmod -R 755 /var/cache/man/'
      # update packages to avoid mandatory update after first boot
    - 'chroot "$1" apt-get update'
      # Setup .bashrc for clean command-line experience
    - 'chroot "$1" cp /etc/skel/.bashrc ~/.bashrc'
      # Enable SSH without password for root user
    - 'echo "PermitRootLogin yes" >> $1/etc/ssh/sshd_config'
    - 'echo "PermitEmptyPasswords yes" >> $1/etc/ssh/sshd_config'

